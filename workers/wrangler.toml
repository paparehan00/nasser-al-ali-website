name = "naa-chat"
main = "chat-worker.js"
compatibility_date = "2024-11-06"

# CORS: restrict to the production domain.
# For quick testing you can temporarily change this to "*".
[vars]
ALLOWED_ORIGIN = "https://alnassertest.sameem.com.np"

# AI_API_KEY is a SECRET, not a plain var. Do NOT put it here.
# Set it with:
#   npx wrangler secret put AI_API_KEY