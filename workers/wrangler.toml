name = "naa-chat"
main = "chat-worker.js"
compatibility_date = "2024-11-06"

# Allowed CORS origins are hardcoded in chat-worker.js (ALLOWED_ORIGINS set).
# AI_API_KEY is a SECRET, not a plain var. Do NOT put it here.
# Set it with:
#   npx wrangler secret put AI_API_KEY